# Jangalekat.UnitTests

Tests unitaires des Handlers `Application` (mocks des interfaces), des règles de calcul (moyennes, mentions, matricules), et des validations FluentValidation. Aucune base de données réelle.
